export interface DefaultLocale {
    endOnSunday: boolean;
    weekDays: string[]
    weekDaysOrderedList: number[]
    months: string[]
    altWeekDays: string[]
    altMonths: string[]
    monthDays: number[]
}